" Turn off compatibility with vi
set nocompatible

" Enable file type detection
filetype on

" Enable loading plugins for specific file types
filetype plugin on

" Enable automatic indentation
filetype indent on

" Set indentation depth to 4 spaces
set tabstop=4
set shiftwidth=4
set expandtab

" Enable line numbers
set number

" Highlight current line
set cursorline

" Enable case insensitive search
set ignorecase

" Enable incremental search
set incsearch

" Enable syntax highlighting
syntax enable

